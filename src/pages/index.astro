---
import Layout from '../layouts/Layout.astro';

const projects = [
  {
    name: "Coral",
    icon: "coral",
    tagline: "Visualize. Understand. Ship.",
    description: "Proto dependency visualizer for gRPC/Connect projects. Interactive graph visualization with package grouping, auto-layout, and GitHub Action integration for CI/CD pipelines.",
    tech: ["Rust", "React", "Axum", "React Flow", "TypeScript"],
    github: "https://github.com/daisuke8000/coral",
    demo: "https://daisuke8000.github.io/coral/",
    accent: "cyan",
    features: ["Interactive Web UI", "GitHub Action", "Pipeline-Ready"]
  },
  {
    name: "mbr-cli",
    icon: "terminal",
    tagline: "Metabase from your terminal.",
    description: "CLI/TUI tool for Metabase API interaction. Execute queries, browse collections, and manage your data analytics platform without leaving the command line.",
    tech: ["Rust", "Tokio", "Ratatui", "Clap", "Async"],
    github: "https://github.com/daisuke8000/mbr-cli",
    accent: "magenta",
    features: ["Rich TUI", "Async Operations", "API Key Auth"]
  },
];

const skills = [
  { name: "Rust", level: 90 },
  { name: "TypeScript", level: 85 },
  { name: "Go", level: 75 },
  { name: "React", level: 85 },
  { name: "Docker", level: 80 },
  { name: "Kubernetes", level: 70 },
];
---

<Layout title="Daisuke | Rust Developer">
  <div class="scanlines"></div>
  <div class="grid-bg"></div>

  <main>
    <!-- Hero Section -->
    <section class="hero">
      <div class="hero-content">
        <div class="terminal-header">
          <span class="terminal-dot red"></span>
          <span class="terminal-dot yellow"></span>
          <span class="terminal-dot green"></span>
          <span class="terminal-title">~/daisuke</span>
        </div>

        <div class="hero-text">
          <p class="prefix">
            <span class="prompt">$</span> whoami
          </p>
          <h1 class="glitch" data-text="DAISUKE">DAISUKE</h1>
          <p class="role">
            <span class="typing">Software Engineer × Rust Enthusiast</span>
            <span class="cursor">_</span>
          </p>
        </div>

        <p class="bio">
          Building developer tools that solve real problems.<br/>
          Crafting CLIs, visualizers, and infrastructure with <span class="highlight-rust">Rust</span>.
        </p>

        <div class="cta-group">
          <a href="#projects" class="btn btn-primary">
            <span class="btn-text">View Projects</span>
            <span class="btn-icon">↓</span>
          </a>
          <a href="https://github.com/daisuke8000" target="_blank" rel="noopener" class="btn btn-secondary">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor">
              <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
            </svg>
            GitHub
          </a>
        </div>
      </div>

      <div class="scroll-indicator">
        <div class="mouse"></div>
        <span>Scroll</span>
      </div>
    </section>

    <!-- Projects Section -->
    <section id="projects" class="projects">
      <div class="container">
        <div class="section-header">
          <span class="section-tag">// FEATURED WORK</span>
          <h2 class="section-title">Projects</h2>
        </div>

        <div class="project-list">
          {projects.map((project, index) => (
            <article class={`project-card ${project.accent}`} style={`--delay: ${index * 0.1}s`}>
              <div class="project-glow"></div>
              <div class="project-content">
                <div class="project-top">
                  <div class="project-icon">
                    {project.icon === "coral" ? (
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                        <path d="M12 3v18M12 3c-2 2-4 4-4 8s2 6 4 8M12 3c2 2 4 4 4 8s-2 6-4 8M5 8c2 0 4 2 7 2s5-2 7-2M5 16c2 0 4-2 7-2s5 2 7 2"/>
                      </svg>
                    ) : (
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                        <rect x="2" y="4" width="20" height="16" rx="2"/>
                        <path d="M6 9l4 3-4 3M12 15h6"/>
                      </svg>
                    )}
                  </div>
                  <div class="project-badges">
                    {project.features.map((f) => (
                      <span class="badge">{f}</span>
                    ))}
                  </div>
                </div>

                <div class="project-info">
                  <h3 class="project-name">{project.name}</h3>
                  <p class="project-tagline">{project.tagline}</p>
                  <p class="project-desc">{project.description}</p>
                </div>

                <div class="project-tech">
                  {project.tech.map((t) => (
                    <span class="tech-pill">{t}</span>
                  ))}
                </div>

                <div class="project-actions">
                  <a href={project.github} target="_blank" rel="noopener" class="action-link">
                    <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
                      <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
                    </svg>
                    Source
                  </a>
                  {project.demo && (
                    <a href={project.demo} target="_blank" rel="noopener" class="action-link primary">
                      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <path d="M18 13v6a2 2 0 01-2 2H5a2 2 0 01-2-2V8a2 2 0 012-2h6M15 3h6v6M10 14L21 3"/>
                      </svg>
                      Live Demo
                    </a>
                  )}
                </div>
              </div>
              <div class="project-border"></div>
            </article>
          ))}
        </div>
      </div>
    </section>

    <!-- Skills Section -->
    <section class="skills">
      <div class="container">
        <div class="section-header">
          <span class="section-tag">// EXPERTISE</span>
          <h2 class="section-title">Tech Stack</h2>
        </div>

        <div class="skills-grid">
          {skills.map((skill, index) => (
            <div class="skill-item" style={`--delay: ${index * 0.05}s`}>
              <div class="skill-header">
                <span class="skill-name">{skill.name}</span>
                <span class="skill-level">{skill.level}%</span>
              </div>
              <div class="skill-bar">
                <div class="skill-progress" style={`--level: ${skill.level}%`}></div>
              </div>
            </div>
          ))}
        </div>

        <div class="tech-cloud">
          <span class="tech-tag">gRPC</span>
          <span class="tech-tag">Protobuf</span>
          <span class="tech-tag">Axum</span>
          <span class="tech-tag">Tokio</span>
          <span class="tech-tag">React Flow</span>
          <span class="tech-tag">Ratatui</span>
          <span class="tech-tag">GitHub Actions</span>
          <span class="tech-tag">Terraform</span>
          <span class="tech-tag">Next.js</span>
          <span class="tech-tag">GraphQL</span>
        </div>
      </div>
    </section>

    <!-- Footer -->
    <footer class="footer">
      <div class="container">
        <div class="footer-content">
          <div class="footer-logo">
            <span class="logo-text">D</span>
          </div>
          <p class="footer-text">
            Built with <span class="heart">♥</span> using Astro
          </p>
          <p class="copyright">© {new Date().getFullYear()} Daisuke</p>
        </div>
      </div>
    </footer>
  </main>
</Layout>

<style>
  @import url('https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;600;700&family=Space+Grotesk:wght@400;500;600;700&display=swap');

  :root {
    --bg-void: #050508;
    --bg-primary: #0a0a0f;
    --bg-card: #0f0f16;
    --bg-elevated: #14141d;
    --text-primary: #e8e8ed;
    --text-secondary: #6b6b7b;
    --text-muted: #3d3d4a;
    --cyan: #00f5ff;
    --cyan-dim: #00a5aa;
    --magenta: #ff00ff;
    --magenta-dim: #aa00aa;
    --yellow: #ffd600;
    --border: rgba(255, 255, 255, 0.06);
    --glow-cyan: 0 0 30px rgba(0, 245, 255, 0.4), 0 0 60px rgba(0, 245, 255, 0.2);
    --glow-magenta: 0 0 30px rgba(255, 0, 255, 0.4), 0 0 60px rgba(255, 0, 255, 0.2);
    --font-display: 'Space Grotesk', sans-serif;
    --font-mono: 'JetBrains Mono', monospace;
  }

  /* Scanlines Overlay */
  .scanlines {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: 1000;
    background: repeating-linear-gradient(
      0deg,
      transparent,
      transparent 2px,
      rgba(0, 0, 0, 0.03) 2px,
      rgba(0, 0, 0, 0.03) 4px
    );
  }

  /* Grid Background */
  .grid-bg {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: -1;
    background-image:
      linear-gradient(rgba(0, 245, 255, 0.03) 1px, transparent 1px),
      linear-gradient(90deg, rgba(0, 245, 255, 0.03) 1px, transparent 1px);
    background-size: 50px 50px;
    mask-image: radial-gradient(ellipse at center, black 0%, transparent 70%);
  }

  main {
    position: relative;
    min-height: 100vh;
    overflow-x: hidden;
  }

  .container {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  /* ===== HERO SECTION ===== */
  .hero {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    position: relative;
    padding: 4rem 2rem;
  }

  .hero-content {
    text-align: center;
    max-width: 700px;
  }

  .terminal-header {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 1rem;
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: 8px 8px 0 0;
    margin-bottom: -1px;
  }

  .terminal-dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
  }
  .terminal-dot.red { background: #ff5f57; }
  .terminal-dot.yellow { background: #ffbd2e; }
  .terminal-dot.green { background: #28c840; }

  .terminal-title {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--text-muted);
    margin-left: 0.5rem;
  }

  .hero-text {
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: 0 0 8px 8px;
    padding: 2rem 3rem;
    margin-bottom: 2rem;
  }

  .prefix {
    font-family: var(--font-mono);
    font-size: 0.875rem;
    color: var(--text-secondary);
    margin-bottom: 0.5rem;
  }

  .prompt {
    color: var(--cyan);
  }

  /* Glitch Effect */
  .glitch {
    font-family: var(--font-display);
    font-size: clamp(3rem, 10vw, 5rem);
    font-weight: 700;
    color: var(--text-primary);
    position: relative;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    line-height: 1;
    margin: 0.5rem 0;
  }

  .glitch::before,
  .glitch::after {
    content: attr(data-text);
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    opacity: 0.8;
  }

  .glitch::before {
    color: var(--cyan);
    animation: glitch-1 2s infinite linear alternate-reverse;
    clip-path: polygon(0 0, 100% 0, 100% 35%, 0 35%);
  }

  .glitch::after {
    color: var(--magenta);
    animation: glitch-2 3s infinite linear alternate-reverse;
    clip-path: polygon(0 65%, 100% 65%, 100% 100%, 0 100%);
  }

  @keyframes glitch-1 {
    0%, 100% { transform: translate(0); }
    20% { transform: translate(-2px, 2px); }
    40% { transform: translate(-2px, -2px); }
    60% { transform: translate(2px, 2px); }
    80% { transform: translate(2px, -2px); }
  }

  @keyframes glitch-2 {
    0%, 100% { transform: translate(0); }
    20% { transform: translate(2px, -2px); }
    40% { transform: translate(2px, 2px); }
    60% { transform: translate(-2px, -2px); }
    80% { transform: translate(-2px, 2px); }
  }

  .role {
    font-family: var(--font-mono);
    font-size: 1rem;
    color: var(--text-secondary);
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.25rem;
  }

  .cursor {
    animation: blink 1s infinite;
    color: var(--cyan);
  }

  @keyframes blink {
    0%, 50% { opacity: 1; }
    51%, 100% { opacity: 0; }
  }

  .bio {
    font-family: var(--font-display);
    font-size: 1.125rem;
    color: var(--text-secondary);
    line-height: 1.7;
    margin-bottom: 2.5rem;
  }

  .highlight-rust {
    color: #ff6b35;
    font-weight: 600;
  }

  .cta-group {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.875rem 1.75rem;
    font-family: var(--font-mono);
    font-size: 0.875rem;
    font-weight: 500;
    text-decoration: none;
    border-radius: 4px;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .btn-primary {
    background: linear-gradient(135deg, var(--cyan-dim), var(--cyan));
    color: var(--bg-void);
    border: none;
  }

  .btn-primary:hover {
    box-shadow: var(--glow-cyan);
    transform: translateY(-2px);
  }

  .btn-secondary {
    background: transparent;
    color: var(--text-primary);
    border: 1px solid var(--border);
  }

  .btn-secondary:hover {
    border-color: var(--cyan);
    color: var(--cyan);
  }

  .scroll-indicator {
    position: absolute;
    bottom: 2rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    color: var(--text-muted);
    font-family: var(--font-mono);
    font-size: 0.75rem;
    animation: float 2s ease-in-out infinite;
  }

  .mouse {
    width: 20px;
    height: 32px;
    border: 2px solid var(--text-muted);
    border-radius: 10px;
    position: relative;
  }

  .mouse::before {
    content: '';
    position: absolute;
    top: 6px;
    left: 50%;
    transform: translateX(-50%);
    width: 3px;
    height: 6px;
    background: var(--cyan);
    border-radius: 2px;
    animation: scroll 1.5s infinite;
  }

  @keyframes scroll {
    0%, 100% { transform: translateX(-50%) translateY(0); opacity: 1; }
    50% { transform: translateX(-50%) translateY(8px); opacity: 0; }
  }

  @keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-5px); }
  }

  /* ===== SECTIONS ===== */
  .section-header {
    margin-bottom: 3rem;
  }

  .section-tag {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--cyan);
    letter-spacing: 0.1em;
    margin-bottom: 0.5rem;
    display: block;
  }

  .section-title {
    font-family: var(--font-display);
    font-size: 2.5rem;
    font-weight: 700;
    color: var(--text-primary);
  }

  /* ===== PROJECTS ===== */
  .projects {
    padding: 6rem 0;
    position: relative;
  }

  .project-list {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .project-card {
    position: relative;
    background: var(--bg-card);
    border-radius: 12px;
    overflow: hidden;
    animation: fadeInUp 0.6s ease forwards;
    animation-delay: var(--delay);
    opacity: 0;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .project-glow {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--cyan), transparent);
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .project-card.cyan .project-glow {
    background: linear-gradient(90deg, transparent, var(--cyan), transparent);
  }

  .project-card.magenta .project-glow {
    background: linear-gradient(90deg, transparent, var(--magenta), transparent);
  }

  .project-card:hover .project-glow {
    opacity: 1;
  }

  .project-border {
    position: absolute;
    inset: 0;
    border-radius: 12px;
    padding: 1px;
    background: linear-gradient(135deg, transparent 40%, var(--cyan) 100%);
    -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
    mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
    -webkit-mask-composite: xor;
    mask-composite: exclude;
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .project-card.magenta .project-border {
    background: linear-gradient(135deg, transparent 40%, var(--magenta) 100%);
  }

  .project-card:hover .project-border {
    opacity: 1;
  }

  .project-content {
    padding: 2rem;
    position: relative;
    z-index: 1;
  }

  .project-top {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 1.5rem;
  }

  .project-icon {
    width: 48px;
    height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 12px;
    background: rgba(0, 245, 255, 0.1);
  }

  .project-card.cyan .project-icon {
    background: rgba(0, 245, 255, 0.1);
    color: var(--cyan);
  }

  .project-card.magenta .project-icon {
    background: rgba(255, 0, 255, 0.1);
    color: var(--magenta);
  }

  .project-icon svg {
    width: 24px;
    height: 24px;
  }

  .project-badges {
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
    justify-content: flex-end;
  }

  .badge {
    font-family: var(--font-mono);
    font-size: 0.625rem;
    padding: 0.25rem 0.5rem;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid var(--border);
    border-radius: 4px;
    color: var(--text-secondary);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .project-info {
    margin-bottom: 1.5rem;
  }

  .project-name {
    font-family: var(--font-display);
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: 0.25rem;
  }

  .project-tagline {
    font-family: var(--font-mono);
    font-size: 0.875rem;
    margin-bottom: 1rem;
  }

  .project-card.cyan .project-tagline { color: var(--cyan); }
  .project-card.magenta .project-tagline { color: var(--magenta); }

  .project-desc {
    color: var(--text-secondary);
    line-height: 1.6;
    font-size: 0.9375rem;
  }

  .project-tech {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-bottom: 1.5rem;
  }

  .tech-pill {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    padding: 0.375rem 0.75rem;
    background: var(--bg-elevated);
    border-radius: 100px;
    color: var(--text-secondary);
    transition: all 0.2s ease;
  }

  .tech-pill:hover {
    color: var(--text-primary);
    background: rgba(255, 255, 255, 0.1);
  }

  .project-actions {
    display: flex;
    gap: 1.5rem;
  }

  .action-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-family: var(--font-mono);
    font-size: 0.875rem;
    color: var(--text-secondary);
    text-decoration: none;
    transition: all 0.2s ease;
  }

  .action-link:hover {
    color: var(--text-primary);
  }

  .action-link.primary {
    color: var(--cyan);
  }

  .project-card.magenta .action-link.primary {
    color: var(--magenta);
  }

  /* ===== SKILLS ===== */
  .skills {
    padding: 6rem 0;
    background: linear-gradient(180deg, transparent, rgba(0, 245, 255, 0.02), transparent);
  }

  .skills-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1.5rem;
    margin-bottom: 3rem;
  }

  .skill-item {
    animation: fadeInUp 0.6s ease forwards;
    animation-delay: var(--delay);
    opacity: 0;
  }

  .skill-header {
    display: flex;
    justify-content: space-between;
    margin-bottom: 0.5rem;
  }

  .skill-name {
    font-family: var(--font-mono);
    font-size: 0.875rem;
    color: var(--text-primary);
  }

  .skill-level {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--cyan);
  }

  .skill-bar {
    height: 4px;
    background: var(--bg-elevated);
    border-radius: 2px;
    overflow: hidden;
  }

  .skill-progress {
    height: 100%;
    width: var(--level);
    background: linear-gradient(90deg, var(--cyan-dim), var(--cyan));
    border-radius: 2px;
    animation: fillBar 1s ease forwards;
    animation-delay: calc(var(--delay) + 0.3s);
    transform-origin: left;
    transform: scaleX(0);
  }

  @keyframes fillBar {
    to { transform: scaleX(1); }
  }

  .tech-cloud {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    justify-content: center;
    padding-top: 2rem;
    border-top: 1px solid var(--border);
  }

  .tech-tag {
    font-family: var(--font-mono);
    font-size: 0.8125rem;
    padding: 0.5rem 1rem;
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: 4px;
    color: var(--text-secondary);
    transition: all 0.2s ease;
  }

  .tech-tag:hover {
    border-color: var(--cyan);
    color: var(--cyan);
    box-shadow: 0 0 15px rgba(0, 245, 255, 0.2);
  }

  /* ===== FOOTER ===== */
  .footer {
    padding: 4rem 0;
    border-top: 1px solid var(--border);
  }

  .footer-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }

  .footer-logo {
    width: 48px;
    height: 48px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, var(--cyan), var(--magenta));
    border-radius: 12px;
  }

  .logo-text {
    font-family: var(--font-display);
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--bg-void);
  }

  .footer-text {
    font-family: var(--font-mono);
    font-size: 0.875rem;
    color: var(--text-secondary);
  }

  .heart {
    color: var(--magenta);
    animation: pulse 1s ease infinite;
  }

  @keyframes pulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.2); }
  }

  .copyright {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--text-muted);
  }

  /* ===== RESPONSIVE ===== */
  @media (max-width: 768px) {
    .hero-text {
      padding: 1.5rem;
    }

    .glitch {
      font-size: 2.5rem;
    }

    .project-content {
      padding: 1.5rem;
    }

    .project-top {
      flex-direction: column;
      gap: 1rem;
    }

    .project-badges {
      justify-content: flex-start;
    }

    .section-title {
      font-size: 2rem;
    }
  }
</style>
